# CHANGELOG.md — Dare to Change (D2C)

> Chronological, oldest first — this reads as the build story, which matters here because later design decisions (e.g. the enrollment-status ripple, the two different denominator conventions) only make sense in light of what came before. Dates are the migration-file timestamps, the most reliable dating source in this repo (no separate release-tagging scheme exists — every push to `main` auto-deploys).

## 2026-07-28 — Foundation
- Initialized Supabase project config, core schema: `profiles` (staff accounts), `classes`, `students`, `qr_tokens`. `is_admin()`/`is_staff()` RLS helper functions established as the pattern for every future policy.
- Attendance pipeline: `attendance_settings` (singleton config), `attendance_logs` (append-only scans), `attendance_days` (canonical daily status), `audit_log`. The scan → trigger → derived-status pipeline (`handle_attendance_scan()`) established as the core mechanism the whole app builds on.
- Python seed script (`generate_seed_sql.py`) written against the school's XEA4402 SIS export + QR card recovery export; initial data imported (614 students, class rosters).
- Flutter scaffold: Student, Class, QR-scan/Attendance features (domain/data/presentation layers), `go_router` wired, initial theme.
- Register QR Card screen + unrecognised-scan-to-register flow.

## 2026-07-29 — QR access widened
- QR card registration/reissue opened to any staff member (was admin-only) — recognized as a routine teacher task, not a disciplinary action.

## 2026-07-30 to 2026-07-31 — Merit module
- Full merit scoring module: `attendance_day_exceptions`, `merit_bonus_points`, `merit_awards`, the `merit_student_daily` **view** (the single computed source of all merit data — no stored ledger). `fn_student_period_summary`/`fn_class_period_summary` for reporting.
- Merit daily roster screen, class summary screen, rewards/recognition screen with 6 award categories, all wired into the router and dashboard tiles.
- Fixed a double-award-logging bug via a `coalesce()`-based unique index (`merit_awards_dedupe_idx`) — a plain multi-column unique constraint doesn't work when exactly one of two nullable scope columns is always null.
- Corrected the "masuk kelas tepat masa" (on-time-to-class) merit point: it is **not** the morning arrival scan, but lateness to any of ~5-6 individual subject periods — added a dedicated `late_to_class` exception flag, decoupled from `attendance_days.status`.

## 2026-08-01 to 2026-08-02 — Session-aware cutoffs, Reports, Settings
- Replaced the single global cutoff time with `session_cutoff_times` (per `pagi`/`petang` session, per ISO weekday) — Forms 1-2 (afternoon session) and Forms 3-5 (morning session) have genuinely different on-time thresholds. The scan trigger was rewritten to look up class session + weekday.
- Reports feature: `fn_weekly_kpi_trend` (attendance rate, repeat-absence count, late/missed-recess trend), explicitly scoped to the 3 of 5 official program KPIs buildable from existing data (mentor coverage and follow-up timeliness deferred — no mentor/case data model exists).
- Settings feature: program period, merit toggles, cutoff-time editing, staff account management (`fn_upsert_staff_by_email` — requires the person to have signed in at least once already; no invite-email flow).

## 2026-08-03 to 2026-08-04 — Manual entry, configurable merit
- Manual attendance entry (`fn_manual_attendance_set`) — single-entry and bulk-migrate UI, for students who didn't scan or for backfilling paper records. Defaults `hadir`/`lewat` time to the class's cutoff when no explicit time is given.
- Per-component merit configurability — each of the 4 components (hadir, tepat masa, kembali rehat, kekal sesi) plus bonus can be individually enabled/disabled per school preference; `merit_max_points` made configurable rather than hardcoded to 4.

## 2026-08-05 — Dashboard v1, responsive shell
- Responsive `AppShell`: persistent sidebar ≥720px width, mobile tile-grid Home below it, with a manual override toggle (`LayoutMode`). Bold/vibrant theme overhaul from stock Material defaults.
- Full Dashboard analytics screen: 6 stat cards with vs-yesterday deltas, attendance trend/status/time-of-day charts, streak leaderboard, class attendance ranking (with a Worst-5 toggle), merit trend/distribution charts, an attendance heatmap, a recent-activity feed, and KPI gauges — 7 backing SQL functions (`fn_attendance_day_summary` through `fn_kpi_overview`), all reading strictly from existing tables/views, no new source-of-truth data.
- Iterated dark mode to match a reference design: gradient page background, glow-accented stat cards, greeting header with date filter, avatar-menu account footer.

## 2026-08-06 — Period summaries, principal-facing reports
- `fn_attendance_period_summary`: day/week/month/year attendance rates at whole-school, Tingkatan, and per-class scope, anchored to a reference date — using a **deliberately fixed, full-period denominator** (not elapsed-days), per an explicit teacher request that the rate read as "progress toward 100%" mid-period. Expanded to add the Tingkatan tier and expose raw present/total counts alongside the percentages (a second table, not just %).
- At-Risk Students report added to Reports — reusing `fn_student_period_summary`, whose denominator convention is deliberately **elapsed/actual-recorded-days**, distinct from the period-summary convention above. This distinction was reasoned through explicitly and is the most important "don't unify these" note in the whole codebase (see `PROJECT.md` §7 #3).
- Home Back Button fix — every non-Dashboard/non-Home screen now has an explicit `HomeBackButton` in its AppBar, because Flutter's default back-arrow silently disappears whenever there's nothing to pop (deep link, page reload), which is exactly the mobile navigation state most users are in.

## 2026-08-07 — More principal-facing signals, Dashboard rearrange
- Chronic Latecomers report added to Reports — backing function `fn_chronic_latecomers` returning students with 3+ late scans in a given period.
- Leave Breakdown report added to Reports — backing function `fn_leave_type_breakdown` giving a per-class summary of `cuti_sakit` and `urusan_rasmi`.
- Dashboard layout rearranged per principal preference: Present card first, Absent second, Late third, Leave fourth. Stat cards click through directly to the corresponding filtered drill-down modal or report page.

## 2026-08-08 — Enrollment status & parent contact overhaul
- Student enrollment status tracking (`EnrollmentStatus`: `active`, `suspended`, `expelled`, `transferred`, `graduated`) added across database schema, RPC functions, Flutter entities, and UI screens.
- `student_guardians` table created with natural keys (`student_id`, `full_name`) allowing safe bulk imports from SIS exports without duplicates. Guardian IC numbers added to enable Parent Portal IC lookup.

## 2026-08-10 — Discipline & Counseling (SSDOP/UBK), Parent Portal IC Lookup, Present Stat Card Alignment
- **Discipline & Counseling Module**: Created PostgreSQL migration `20260811000002_discipline_and_counseling.sql` establishing `discipline_records` and `counseling_records` tables with index structures, RLS policies, and summary RPC `fn_student_discipline_summary(p_student_id)`. Created `DisciplineCounselingScreen` with 4 tabs (*Kes Disiplin SSDOP*, *Sesi Kaunseling UBK*, *Peti Suara Murid*, and *Ringkasan & Analisis*). The `disiplin` (**Guru Disiplin**) and `kaunselor` (**Guru Kaunselor**) roles described in `DISCIPLINE_AND_COUNSELING.md` are a UI/process convention — **"RBAC security enforcement" as originally written here overstated it**; `profiles.role` has no such values and every RLS policy on these tables is staff-wide (`to authenticated using (true)`), not gated to either role. Corrected 2026-09-13, see `KNOWN_ISSUES.md` KI-015.
- **Parent Portal IC Lookup**: Created `fn_parent_portal_data_by_ic(p_ic_number)` allowing guardians to access real-time child attendance & merit progress by keying in their MyKad / IC number (`/#/parent`), adding direct shortcut button on main `SignInScreen`.
- **Dashboard Stat Card Alignment**: Updated `fn_attendance_day_summary` to include `cuti_sakit` and `urusan_rasmi` under present count breakdown (`328 Total Present: 320 Hadir • 7 MC • 1 Rasmi`) matching filter chip counts 100%.

## 2026-08-11 — Student Portal & Student Voice (Suara Murid), High-Tech Landing Page
- **Student Portal & Student Voice**: Created `StudentPortalScreen` at route `/#/student` authenticated securely via **Student Name Tag QR Code camera scan or QR token entry** (eliminating IC impersonation). Created `student_voice_submissions` table and `fn_student_portal_data_by_qr(p_qr_token)` RPC querying `public.qr_tokens`. Allows students to track attendance %, merit points, and submit **Suara Murid** feedback or anti-bullying reports with optional **Anonymous (Sulit/Rahsia)** protection. **The anonymity/confidentiality promise made here is not actually enforced at the database level** — `student_voice_submissions` grants unrestricted `select` to `anon` (not caught at the time this shipped; flagged 2026-09-13, see `KNOWN_ISSUES.md` KI-014, currently the top-priority open issue in this codebase).
- **Peti Suara Murid (Teacher Inbox)**: Integrated student voice submission inbox into `DisciplineCounselingScreen` where UBK, Discipline, and Admin staff can respond to student voices and post official action notes.
- **SMK Sungai Damit & D2C Project Public Web Landing Page**: Created `SchoolLandingScreen` at root `/#/` and `/#/landing` with a high-tech, futuristic aesthetic (deep navy & cosmic violet gradient, glassmorphic floating header bar, pulsing system status dot, neon cyan-gold typography, 3 Aras Intervensi grid, 4-step daily merit routine, whole-school T1–T5 scope, official school leadership list, and subtle portal launchpad buttons).

## 2026-08-17 — School Announcements (Discipline & Counseling UBK) & Student Portal Pengumuman Tab
- **Database Migration (`20260817000001_school_announcements.sql`)**: Created `school_announcements` table and updated `fn_student_portal_data_by_qr` RPC to automatically include live published announcements in student portal payloads.
- **Relocated Special Announcement Composer**: Removed ad-hoc announcement composer card from `ReportsScreen`.
- **Dual Announcement Composers in Disiplin & Kaunseling (`/#/discipline-counseling`)**: Added **Pengumuman Disiplin** composer in Tab 1 (*Kes Disiplin SSDOP*) and **Pengumuman Kaunseling (UBK)** composer in Tab 2 (*Sesi Kaunseling UBK*). Teachers can compose announcements, optionally attach a student, publish directly to the student portal database, and copy text for WhatsApp/PIBG.
- **Portal Murid Tab 1 (📢 Pengumuman)**: Added **📢 Pengumuman** as the **1st Tab** (before *Kemajuan Saya*) in `StudentPortalScreen` (`/#/student`) displaying live Discipline and Counseling announcements.

## 2026-08-16 — D2C User Manual
- `docs/MANUAL_PENGGUNA_D2C.md` + `.docx` (T-044) — a complete end-user manual, separate audience/purpose from this AI Development Kit (`docs/`).

## 2026-08-17 (later) — Landing Page "Cyber" Redesign, D2C AI Assistant, Sudut Info
- **Landing Page redesign (T-045)**: `SchoolLandingScreen` rebuilt to match a supplied reference mockup pixel-for-pixel — every widget renamed/reworked from `_Tech*` to `_Cyber*`, a real photo hero background (`assets/images/school_front.jpg`, 0.28 opacity), and the hero split into a two-column layout with a live Sudut Info card moved into the right column as a manual-advance carousel. See `LANDING_PAGE.md`.
- **D2C AI Assistant (T-046)**: `D2CAiAssistantDialog` + `D2CAiAssistantService`, opened from a new "PEMBANTU AI" FAB on the Landing Page. The dialog UI reads "GEMINI INTELLIGENCE • ONLINE" and the service does contain a real Gemini API call path — but it is never given an API key anywhere in this codebase or its deploy workflow, so every response in production comes from a local keyword-matching fallback. See `KNOWN_ISSUES.md` KI-016; same-day follow-up commit refined the local fallback's keyword matching for tingkatan-specific questions.
- **Special Announcement Management list**: view/edit/delete/unpublish for previously-published announcements, added to the Discipline & Counseling screen.
- **Sudut Info (T-047)**: `sudut_info_posts` table with a scheduled display window (`valid_from`/`valid_until`), `RichTextToolbarWidget` for HTML-tag text formatting (bold/italic/underline/highlight/lists — raw tags, not WYSIWYG), and `fn_active_sudut_info_posts()`. Consumed by both the Landing Page hero card and the Student Portal.

## 2026-08-18 — Sudut Info Graphics & Storage Hardening (T-048)
- Poster/banner graphic support: `sudut_info_posts.image_url`, direct `file_picker` upload to a new public `sudut-info-banners` Storage bucket (the project's first and only Storage bucket), preset template chips, and a live preview in both the create composer and edit dialog.
- Auto-delete of the previously-uploaded storage file when an image is cleared or a post is deleted, plus the storage `delete` RLS policy that was missing for a few hours after upload first shipped (uploads were write-only in the interim). **This logic lives directly in `discipline_counseling_screen.dart` against `Supabase.instance.client.storage`, bypassing `DisciplineCounselingRepository`** — the only place in the app that talks to Supabase outside a repository. See `KNOWN_ISSUES.md` KI-017.

## 2026-09-13 (correction + docs catch-up) — Reconciling a month of undocumented shipped work
- **No code changes** — a pure documentation pass, prompted by the user noticing the docs hadn't kept up with the 2026-08-16 to 2026-08-18 work above (User Manual, the Landing Page "Cyber" redesign, the D2C AI Assistant, and Sudut Info end-to-end), and asking to "revise everything... about the latest [state of] this system."
- **Found two genuine issues while doing this pass, not previously tracked anywhere**: `student_voice_submissions` grants `anon` unrestricted `select` (`KNOWN_ISSUES.md` KI-014 — treat as the top-priority open item in this codebase, confidential/anonymous bullying reports are not actually confidential from anyone holding the public anon key), and the D2C AI Assistant's "GEMINI INTELLIGENCE" branding has never had an API key wired to it (KI-016). Also documented: Discipline & Counseling / Announcements / Sudut Info RLS is staff-wide, not role-gated as the module's own docs imply (KI-015), and Sudut Info's image storage calls bypass the repository layer (KI-017).
- Corrected two overstated claims already in this file (see the inline notes on the 2026-08-10 and 2026-08-11 entries above) rather than rewriting history — the original entries stay, with what turned out to be wrong flagged in place.
- Rewrote `DISCIPLINE_AND_COUNSELING.md` and `LANDING_PAGE.md` from scratch (both had drifted far enough from shipped reality — including a stale "not implemented yet" banner on the former — that patching in place wasn't practical). Updated `DATABASE.md` (5 missing tables + their functions + the `sudut-info-banners` storage bucket; the file's own migration-range header was 8 migrations behind), `API.md`, `COMPONENTS.md`, `PROJECT.md` (§3 module table, §4 tech stack, §7 design decisions #8-10, §11 roadmap contradiction), `STUDENT_PORTAL_AND_VOICE.md`, and `TASKS.md` (added T-043 through T-048 for the previously-untracked work; removed a stale, ID-colliding `T-041` entry from the Blocked table — see that file's own correction note).

## 2026-09-14 — Sudut Info moved to Student/Parent Portals; KI-014 and KI-013 fixed
- **Sudut Info re-scoped** (per Raizal: it belongs in the Student Portal and Parent Portal, not the public Landing Page, and the two audiences shouldn't see the same posts): added `sudut_info_posts.audience` (`murid`/`ibu_bapa`/`kedua_dua`, migration `20260914000002`), an "Untuk Siapa" picker in the Discipline & Counseling composer/edit dialog, and an audience badge on the management list. Removed the Sudut Info card entirely from `SchoolLandingScreen`'s hero (back to a single-column `StatelessWidget`, no Riverpod dependency). Added a live `audience=murid` feed to the Student Portal's "Inspirasi" tab and an `audience=ibu_bapa` section to `ParentPortalBody` (shared by both Parent Portal entry points).
- **`supabase/.env`** (gitignored, `.env.example` committed alongside it) now holds a working `SUPABASE_ACCESS_TOKEN`, ending the month-long CLI/Management-API access blocker (KI-013's original root cause).
- **KI-014 fixed and verified**: migration `20260914000001` applied to production. Verified with `set local role anon; select count(*) from student_voice_submissions;` → `0`, regardless of actual row count. Confidential Suara Murid submissions are no longer world-readable.
- **KI-013 fixed and verified**: Supabase Auth `site_url`/`uri_allow_list` were still pointing at the dead `https://cgfusion.github.io/summit` domain (confirmed via `GET /v1/projects/.../config/auth`) — a full month after the domain migration. Fixed via `PATCH` to the same endpoint, set to `https://d2csummit.online`; re-verified via a follow-up `GET`. Invite-staff and password-reset email links now resolve correctly.
- Fixed a stale line in the Sudut Info composer's helper text still claiming posts show on the public Landing Page.
- Fixed Sudut Info image display: images in the Discipline & Counseling management list and the Landing-Page-turned-portal cards were being cropped to a fixed aspect ratio instead of showing the full image, and the "Inspirasi" tab's static quote card had yellow text on a white/light background (hard to read). Images are now shown at their natural aspect ratio and are tappable to open full-screen (`core/widgets/full_screen_image_viewer.dart`).
- **Sudut Info also added to both portals' pre-login screens** (same day, after the above): Raizal clarified the posters' QR codes point straight at the login screens (`ParentIcLookupScreen`, the Student Portal's QR/token entry view) — a visitor who never authenticates was missing Sudut Info entirely under the post-login-only placement above. Added `_ParentLoginSudutInfoSection`/`_StudentLoginSudutInfoSection`, reusing the same `activeSudutInfoPostsProvider` query as the post-login views; both placements now coexist. Made `ParentSudutInfoCard` public (was `_ParentSudutInfoCard`) so the login-screen file could reuse it instead of duplicating the card widget.
- While verifying this live, found that both currently-published Sudut Info posts had a `valid_from` several hours in the future relative to the database's UTC `now()`, so neither showed on either portal — confirmed this was a real display state, not a bug in the new login-screen code, by temporarily setting one post's `valid_from` to the past, screenshotting the card rendering correctly, then reverting it. Tracked as KI-018 and **fixed the same day** (below).
- **KI-018 fixed**: `createSudutInfoPost`/`updateSudutInfoPost`/`getSudutInfoPosts` (`discipline_counseling_repository_impl.dart`) were serializing local `DateTime`s to Postgres via bare `.toIso8601String()` with no `.toUtc()` first — Postgres's `timestamptz` column silently interpreted the local (Malaysia, UTC+8) wall-clock digits as UTC, shifting every stored `valid_from`/`valid_until` (and the "active" query's own `nowStr`) ~8 hours later than intended. Fixed by adding `.toUtc()` before `.toIso8601String()` in all three call sites. Also corrected the two already-published rows' `valid_from` by `- interval '8 hours'` to their true originally-intended instant (both then fell before real `now()`, confirming they were meant to be immediately active). Re-verified live with **no manual data patching**: both posts now render correctly on the Parent/Student login screens straight from the fixed code path. See `KNOWN_ISSUES.md` KI-018 for the full writeup.

## 2026-09-15 — SAFE Anti-Bullying Questionnaire (T-050)
- **`safe_questionnaire_responses`** (migration `20260915000001`): 12 Likert-scale items (1-5) across 3 fixed sections (Pengetahuan & Kesedaran Antibuli / Amalan Sekolah Penyayang (6S) / Peranan PRS), one row per student (`student_id unique`, upsert-on-resubmit — no answer history is kept). Source instrument: `docs/SOAL_SELIDIK_PENILAIAN_PROGRAM_SCHOOL_ANTIBULLIYING_FRAMEWORK.docx`.
- **Corrected a scoring error in the source document**: it specifies `Peratus Skor = (Jumlah Skor / 40) x 100`, but 12 items × 5 points max is a 60-point scale, not 40 — confirmed with Raizal as a documentation error in the source material. All three places that compute this score (`fn_get_my_safe_questionnaire`, `fn_safe_questionnaire_summary`, and the Dart `SafeQuestionnaireResponseRow.fromMap` for the staff drill-down) use `/60` and derive level/`memahami` from the resulting percentage (`<50% rendah`, `50-74% sederhana`, `>=75% tinggi`/`memahami`), not from the source's raw-score bands, which don't tile onto 12 items anyway.
- **Security pattern deliberately different from `student_voice_submissions`'s original mistake (KI-014)**: `safe_questionnaire_responses` grants `anon` **no direct table access at all**, not even a scoped one. All writes go through `fn_submit_safe_questionnaire` (`security definer`), which resolves and validates the calling student's QR token *inside the function* before touching the table. This is the pattern an unauthenticated-identity write should have used from the start.
- **Student-facing**: new "SAFE" tab (5th) in the Student Portal — a 12-question Likert form on first visit, a scored results view (with an edit-to-resubmit option) after.
- **Staff-facing**: new "Soal Selidik SAFE" tab (6th) in Discipline & Counseling — read-only aggregate stats, a level-distribution bar, per-section score bars, and an individual-response list.
- Added `app/test/unit/safe_questionnaire_test.dart` (5 tests, all covering the `/60`-not-`/40` correction and the level-threshold logic) and `docs/pelancaranProgramSafe.jpg`/`docs/pelancarand2c.jpg` as reference launch-event photos alongside the source `.docx`.

## 2026-09-14 (later) — Saringan Minda Sihat tab (T-051), tab bar layout fix
- **New 6th Student Portal tab, "Saringan Minda Sihat"**: a consent gate in front of the Ministry of Education's (KPM) external mental-health screening (`https://sepkm.com/msihatmenengah`) — per Raizal's exact spec (3 numbered instructions, a checkbox, a link button). No new table or RPC — purely client-side UI, nothing is recorded about whether a student opened or completed the external screening.
- Added `url_launcher` as a direct dependency (was only a transitive one before) to open the external link in a new tab via `launchUrl(uri, webOnlyWindowName: '_blank')`.
- Iterated on the exact UI per feedback mid-build: the "understood" control started as a button, changed to a `CheckboxListTile` labeled "SAYA FAHAM" per Raizal ("i think Faham is check box much better"); the "SARINGAN MINDA SIHAT" button stays disabled until it's ticked.
- **Tab bar layout fix (same day, immediate follow-up)**: with 6 tabs the previous scrollable `TabBar` cut off "Saringan Minda Sihat", requiring a horizontal scroll to reach it or "SAFE" — reported directly via an annotated screenshot. Switched to a non-scrollable, evenly-distributed `TabBar` with a custom `_wrappingTab()` helper (icon + a small, up-to-2-line `Text`), so all 6 tabs fit on one row at any width without scrolling.

## 2026-09-15 — Whole-school QR card rollout + 224-student token mismatch fix
- **Student master refresh**: `XEA4402 Keseluruhan Murid as of 2026-09-15.xlsx` (614 → 616 students after upsert; 2 new enrollments), applied via `scripts/generate_seed_sql_2026-09-15.py` → `supabase/seed/seed_data_2026-09-15.sql`, same upsert pattern as the original `generate_seed_sql.py`.
- **QR card coverage expanded school-wide**: previously only 252 students across 9 "D2C" classes (Tingkatan 1-2) had a card; now all 609 students the school has cards for, across 21 classes / Tingkatan 1-5, per Raizal ("last time only Tingkatan 1 and Tingkatan 2, now the QR is for all tingkatan from 1 to 5").
- **Found and fixed a real data-integrity bug in the original 252-token import**: 224 of those 252 tokens were linked to the *wrong* student — not reissued cards, the exact same physical codes, just mismatched. Root cause: the original `QR_Master_Merge_Result.xlsx` import (months ago) matched two spreadsheets by row position, not by identity. Caught this by diffing the new XEA4402 file's own embedded "QR Token" column against the live database and finding a huge (91%) mismatch rate that turned out to be mostly clean pairwise/cyclic swaps of *already-known* token values — a strong signal of a row-order bug, not a genuine reissue. **The XEA4402 file's own QR Token column turned out to be unreliable too** (disagreed with the actual class-sheet scan output on ~half its rows) — Raizal pointed to the real source, `D:\Summit\System\qr_recovery\NewQR\qr_output\results_named.csv` (grouped by class PDF + row number + name + code, zero duplicate names or codes), which was used as the authoritative mapping instead, matched by name (601 exact matches + 7 manually-verified spelling-variant fixups, e.g. "JIPIN"/"JAIPIN" + 1 brand-new student not yet in the roster).
- **Migration required a two-phase approach**, not a plain revoke-then-insert: because most of the "wrong" tokens were swaps of values *already in the table*, a naive insert hit the `qr_tokens_token_key` unique constraint (a revoked row still holds its token text). Fixed by (1) revoking each affected student's current wrong active row first, freeing their `qr_tokens_one_active_per_student` slot, then (2) `insert ... on conflict (token) do update set student_id = excluded.student_id, status = 'active'` for all 609 authoritative pairs — this relinks each physical card's existing row to its true owner rather than creating a new row, and only genuinely-retired old codes (13 of them) end up permanently `revoked`.
- **Verified live** via the real `fn_student_portal_data_by_qr` RPC (not just SQL): the previously-mislinked token now correctly resolves to HAZEL MERYCHELL EBLE (its true owner) instead of AHMAD FARISH DANIAL BIN MORRIS, and his own corrected token resolves to him. `qr_tokens` ends at 612 active (609 authoritative + 3 students who have a token but no longer appear in the new master file, left untouched per Raizal — no assumption made about them having left the school).
- **This "fix" was itself wrong on 540 of 609 rows, caught the same day (see KI-019 below)**: Raizal physically scanned a real card (ADDY AERAYYAN ARASSH BIN ANNUAR's) and it resolved to a different student (JANE SHERLYN JOHNSON). `results_named.csv` had reversed the QR-to-name pairing on every class page — it was built by decoding each page's QR codes with pyzbar (which returns detections in internal-algorithm order, not reading order) and zipping that raw order directly against the alphabetically-sorted name list. Confirmed by decoding all 83 source PNGs directly, sorting each page's detections into true top-to-bottom/left-to-right order, and finding it was the exact reverse of what `results_named.csv` had recorded. Corrected via `scripts/fix_qr_reading_order_2026-09-15.py` → `supabase/seed/fix_qr_reading_order_2026-09-15.sql`, same two-phase revoke-then-upsert approach as the first pass. **Re-verified this time against the literal value decoded from the real card in question**, plus 5 further random cross-class spot checks — all correct. The earlier "verified live via the real RPC" claim above was misleading: it only proved the database was internally consistent with what had just been written to it, not that the mapping was correct against reality. See `KNOWN_ISSUES.md` KI-019 for the full methodological lesson.
